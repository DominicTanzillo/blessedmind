Start my daily planning session. Do the following steps:

1. Call `mcp__blessedmind__get_dashboard` to get today's summary
2. Call `mcp__blessedmind__list_grinds` to see my habits
3. Call `mcp__blessedmind__list_tasks` with `completed: false, limit: 20` to see all open tasks

Then present a clear daily briefing:
- Which habits are done vs pending today
- My current focus batch (top 3 tasks)
- Any overdue tasks that need attention
- Tasks due today

After the briefing, suggest a plan for the day:
- What to tackle first based on priority and due dates
- How to approach the top task (break it down if it has steps)
- Estimate how many pomodoro sessions the day might need

Ask me if I want to adjust priorities, add tasks, or start working on something specific.
